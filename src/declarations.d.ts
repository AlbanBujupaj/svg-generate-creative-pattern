declare module '@georgedoescode/generative-utils' {
  export function map(
    value: number,
    inMin: number,
    inMax: number,
    outMin: number,
    outMax: number
  ): number;

  // Add more function declarations here if needed
}
